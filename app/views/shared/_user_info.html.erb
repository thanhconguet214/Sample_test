<%= link_to gravatar_for(current_user, size: Settings.maximum1), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t("shared._user_info.view_my_profile"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, t("shared._user_info.micropost")) %></span>
